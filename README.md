# image-prediction-api
Developed an Image Prediction API using FastAPI, Scikit-learn, NumPy, and Pillow. The API accepts image uploads, preprocesses images, loads a trained machine learning model, and returns predictions through REST endpoints. Swagger UI was used for interactive API testing and documentation.
